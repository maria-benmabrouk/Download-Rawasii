function showMessage(system) {
  document.getElementById("msg").innerText =
  `${system} version coming soon`;
}
function showMessage2(system) {
  document.getElementById("msg2").innerText =
  `${system} version coming soon`;
}